# Agents

Sample Go repo agent navigation file.

## Entry points

- `cmd/main.go` — CLI entry point
- `internal/service/` — domain service package

## Build

```bash
go build ./cmd/...
go test ./...
```
